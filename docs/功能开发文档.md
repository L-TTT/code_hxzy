# 和弦之约 - 功能开发文档

| 项目 | 内容 |
|------|------|
| 项目名称 | 和弦之约（Melody of Love） |
| 文档版本 | v6.0 |
| 更新日期 | 2026-05-03 |
| 云环境 ID | toust-d9gu1e0km340d2d2f |
| AppID | wx3d369e6a4877df74 |

---

## 一、项目现状

### 云函数实现状态

| 云函数 | 文件 | 状态 | 说明 |
|--------|------|------|------|
| login | index.js, package.json | ✅ 已完成 | openid 校验 + 新用户检测 |
| register | index.js, package.json | ✅ 已完成 | 新用户注册，含角色选择 |
| getUserInfo | index.js, package.json | ✅ 已完成 | 获取用户信息 |
| eventManager | index.js, package.json | ✅ 已完成 | 完整 CRUD + 权限校验（create/update/delete/get/list/batchCreate） |
| uploadManager | index.js, package.json | ✅ 已完成 | 云存储路径生成 + 临时 URL + 文件删除 |
| adManager | index.js, package.json | ✅ 已完成 | 积分查询 + 扣除 + 广告观看记录（+30积分） |
| shareManager | index.js, package.json | ✅ 已完成 | 分享卡片生成 + 分享记录 |
| pdfExporter | index.js, package.json | ✅ 已完成 | 事件数据导出 + 照片临时 URL 解析 |

### 页面实现状态

| 页面 | 文件 | 状态 | 说明 |
|------|------|------|------|
| login | login.ts, login.wxml, login.wxss | ✅ 已完成 | 登录检测 + 注册表单（昵称、头像、角色） |
| home | home.ts, home.wxml, home.wxss | ✅ 已完成 | 动态画册渲染（封面+事件+封底） + 3D翻页 + 触摸滑动 + 音乐播放联动 + PDF导出Canvas渲染 + 分享海报 + 积分下载 + 照片URL解析 + 懒加载 + 低端机型降级 |
| mine | mine.ts, mine.wxml, mine.wxss | ✅ 已完成 | 接入 eventManager 云函数 + 筛选/新增/编辑/删除事件 + 年份分组 |
| settings | settings.ts, settings.wxml, settings.wxss | ✅ 已完成 | 主题/自动播放/背景音乐/谱线样式设置持久化 + 备份/导出PDF/清除缓存 + 原生分享 |
| event-detail | event-detail.ts, event-detail.wxml, event-detail.wxss | ✅ 已完成 | 云函数加载事件 + 编辑跳转event-edit + 云端删除 + 音乐播放 + 照片预览 + 分享 |
| event-edit | event-edit.ts, event-edit.wxml, event-edit.wxss | ✅ 已完成 | 完整表单（标题/日期/类型/描述/照片上传/音乐/地点），支持创建和编辑模式 |
| index | index.ts, index.wxml, index.wxss | ✅ 已完成 | 默认模板页（未使用） |
| logs | logs.ts, logs.wxml, logs.wxss | ✅ 已完成 | 日志页 |

### 组件实现状态

| 组件 | 文件 | 状态 | 说明 |
|------|------|------|------|
| note-card | note-card.ts, note-card.wxml, note-card.wxss | ✅ 已完成 | 音符卡片组件，支持多种类型样式 |
| mini-player | mini-player.ts, mini-player.wxml, mini-player.wxss | ✅ 已完成 | 迷你音乐播放器（播放/暂停/上下曲/进度条/时间显示） |

### 配置与基础设施

| 项目 | 状态 | 说明 |
|------|------|------|
| 云开发环境 | ✅ 已配置 | env: toust-d9gu1e0km340d2d2f |
| 页面路由 | ✅ 已配置 | app.json 注册 8 个页面 |
| TabBar | ✅ 已配置 | 画册(home) + 管理(mine) 两个 Tab |
| 导航栏适配 | ✅ 已完成 | 所有页面动态适配状态栏 + 胶囊按钮 |
| 用户系统 | ✅ 已完成 | app.ts 启动时自动检测登录状态 |
| 全局数据 | ✅ 已配置 | globalData 含 userInfo/theme/adEnabled/adUnitId |

### 功能实现率

| 分类 | 总数 | 已完成 | 部分完成 | 待开发 | 完成率 |
|------|------|--------|----------|--------|--------|
| 用户系统 | 3 | 3 | 0 | 0 | 100% |
| 云函数 | 8 | 8 | 0 | 0 | 100% |
| 画册渲染 | 5 | 5 | 0 | 0 | 100% |
| 事件管理 | 4 | 4 | 0 | 0 | 100% |
| 媒体管理 | 6 | 6 | 0 | 0 | 100% |
| 交互功能 | 5 | 5 | 0 | 0 | 100% |
| 分享导出 | 3 | 3 | 0 | 0 | 100% |
| 高级功能 | 6 | 6 | 0 | 0 | 100% |
| **合计** | **40** | **40** | **0** | **0** | **100%** |

### 详细功能清单

#### ✅ 已完成

| 功能 | 实现位置 | 说明 |
|------|---------|------|
| 用户登录检测 | app.ts → login.ts | 启动时调用 login 云函数检测，已注册用户自动跳转 home |
| 用户注册 | login.ts | 昵称 + 头像 + 角色（男/女）注册 |
| 云函数 CRUD | eventManager/index.js | 完整 6 个 action，含权限校验 |
| 云存储管理 | uploadManager/index.js | 路径生成 + 临时 URL + 删除 |
| 积分系统 | adManager/index.js | 查询/扣除/广告+30 |
| 画册动态渲染 | home.ts → home.wxml | pages 数组驱动，封面/事件/封底三类页面 wx:for 渲染 |
| 3D翻页交互 | home.ts + home.wxss | swiper + touchstart/touchend 滑动 + flipAnimation |
| 音乐播放联动 | home.ts → playMusic/pauseMusic | innerAudioContext 实际播放 + 进度条 + 自动下一曲 |
| 事件列表云接入 | mine.ts → loadEvents | eventManager.list 加载 + enrichEvent 计算衍生字段 |
| 事件新增(云) | mine.ts → confirmAdd | eventManager.create 云端创建 |
| 事件删除(云) | mine.ts → deleteEvent | eventManager.delete 二次确认 + 云端删除 |
| 事件编辑跳转 | mine.ts → showEventMenu | 编辑跳转 event-edit?id=xxx |
| 详情页云加载 | event-detail.ts → loadEventData | eventManager.get 按 ID 加载 |
| 详情页编辑 | event-detail.ts → editEvent | 跳转 event-edit?id=xxx |
| 详情页删除 | event-detail.ts → deleteEvent | eventManager.delete + navigateBack |
| 详情页音乐 | event-detail.ts → playMusic | innerAudioContext 实际播放 |
| 详情页照片预览 | event-detail.ts → previewPhoto | wx.previewImage 大图浏览 |
| PDF导出Canvas | home.ts → renderPDFToAlbum | Canvas 绘制事件列表 + saveImageToPhotosAlbum |
| 分享海报 | home.ts → sharePage + drawAndSavePoster | shareManager + Canvas 海报 + 保存相册 |
| 原生分享 | home/settings/event-detail → onShareAppMessage | 微信原生分享给好友 |
| 设置持久化 | settings.ts → loadSettings/saveSettings | Storage 持久化主题/自动播放/背景音乐/谱线样式 |
| 备份功能 | settings.ts → backupData | eventManager.list 获取全量数据存入 Storage |
| 导出PDF(设置) | settings.ts → exportPdf | pdfExporter 云函数调用 |
| 清除缓存 | settings.ts → clearCache | wx.clearStorageSync + UI 重置 |
| 关于/帮助 | settings.ts → openAbout/openHelp | 弹窗展示应用信息和帮助 |
| 分享卡片数据 | shareManager/index.js | 生成分享数据 + 记录 |
| PDF 数据导出 | pdfExporter/index.js | 事件列表 + 用户信息 + 照片 URL |
| 事件编辑表单 | event-edit.ts | 完整表单 + 照片上传 + 地点选择 |
| 3D 翻页基础 | home.ts | 翻页动画 + 触摸滑动 + 防抖 |
| 迷你播放器 | mini-player 组件 | 完整播放控制 + 进度条 |
| 音符卡片 | note-card 组件 | 多类型样式支持 |
| 分享海报前端 | home.ts → sharePage() | 调用 shareManager + Canvas 绘制 + 保存相册 |
| 下载积分检查 | home.ts → downloadPage() | 查询积分 + 积分不足提示 |
| 广告观看 | home.ts → showRewardedVideoAd() | 激励视频广告 + 积分发放 |
| 积分扣除 | home.ts → consumePointsAndDownload() | 30 积分扣除 |
| 照片URL解析 | home.ts → resolvePhotoUrls | uploadManager.getTempUrl 批量将 fileId 转临时 URL |
| 照片URL解析(详情) | event-detail.ts → resolvePhotoUrls | 同上，详情页加载后解析 |
| 图片懒加载 | home/event-detail/mine wxml | 所有 `<image>` 添加 lazy-load 属性 |
| 照片 fileId 兼容 | home/event-detail/mine wxml | wxml 同时支持 `url` 和 `fileId` 两种照片源 |
| 低端机型降级 | home.ts → checkPerformance | benchmarkLevel/Android版本/屏幕宽度检测，自动禁用翻页动画 |

#### ⚠️ 部分完成（前端已编写但未完全集成或功能未完善）

| 功能 | 实现位置 | 当前状态 | 待完善 |
|------|---------|---------|--------|
| 乐谱渲染增强 | note-card 组件 | 基础音符卡片已实现 | 需五线谱 + 音符卡片动态渲染 |

---

## 二、2 天冲刺开发计划

> 目标：两天内完成核心功能闭环，实现可演示的 MVP 版本。

### Day 1：后端 + 事件管理（约 10 小时）

| 序号 | 任务 | 工时 | 依赖 | 交付物 |
|------|------|------|------|--------|
| 1.1 | 创建 events 数据库集合 | 0.5h | 无 | 集合创建完成 |
| 1.2 | 完善 eventManager 云函数 | 1.5h | 1.1 | CRUD + 权限校验 |
| 1.3 | 完善 uploadManager 云函数 | 1h | 无 | 上传凭证生成 |
| 1.4 | 完善 adManager 云函数 | 0.5h | 无 | 积分管理 |
| 1.5 | 补充 pdfExporter 云函数 | 0.5h | 无 | index.js |
| 1.6 | 创建 event-edit 页面 | 2h | 1.2 | 完整表单页 |
| 1.7 | 实现事件 CRUD 前端 | 2h | 1.2, 1.6 | 创建/编辑/删除 |
| 1.8 | 集成照片上传 | 2h | 1.3, 1.7 | 批量上传 + 预览 |

### Day 2：前端增强 + 测试（约 10 小时）

| 序号 | 任务 | 工时 | 依赖 | 交付物 |
|------|------|------|------|--------|
| 2.1 | 完善 3D 翻页效果 | 2h | 无 | CSS 3D 动画 |
| 2.2 | 实现音乐播放器 | 2h | 无 | mini-player 组件 |
| 2.3 | 完善分享海报功能 | 1.5h | 1.4 | Canvas 海报 |
| 2.4 | 完善广告接入前端 | 1h | 1.4 | 激励视频 |
| 2.5 | 完善 PDF 导出前端 | 1h | 1.5 | 导出 + 预览 |
| 2.6 | 整体联调测试 | 1.5h | 全部 | 功能验证 |
| 2.7 | Bug 修复 + 优化 | 1h | 2.6 | 稳定版本 |

### 开发顺序依赖图

```
Day 1:
  events 集合 → eventManager 云函数 → event-edit 页面 → 事件 CRUD 前端
                                                          ↓
  uploadManager 云函数 ─────────────────────────→ 照片上传集成

Day 2:
  3D翻页 ←── home.ts 已有基础
  mini-player ←── 独立组件
  shareManager ──→ 分享海报前端
  adManager ──→ 广告接入前端
  pdfExporter ──→ PDF 导出前端
```

---

## 三、数据库设计

### events 集合

```json
{
  "_id": "自动生成",
  "openid": "用户 openid（权限控制字段）",
  "title": "事件标题（必填）",
  "date": "2024-02-14（必填，YYYY-MM-DD）",
  "description": "事件描述",
  "type": "daily | birthday | anniversary | special",
  "photos": [
    {
      "fileId": "云存储 fileID",
      "description": "照片描述",
      "order": 0
    }
  ],
  "music": {
    "title": "音乐标题",
    "artist": "艺术家",
    "fileId": "云存储 fileID",
    "url": "外部链接（与 fileId 二选一）"
  },
  "location": {
    "name": "地点名称",
    "latitude": 39.9042,
    "longitude": 116.4074
  },
  "sort": 0,
  "createdAt": "serverDate",
  "updatedAt": "serverDate"
}
```

### users 集合扩展字段

```json
{
  "points": 0,
  "adWatchCount": 0,
  "lastAdWatchTime": null
}
```

### share_records 集合

```json
{
  "_id": "自动生成",
  "openid": "用户 openid",
  "eventId": "事件 ID",
  "sharedAt": "serverDate"
}
```

---

## 四、云函数接口规范

### 4.1 eventManager

> 事件管理云函数，支持 CRUD + 批量操作 + 权限校验

**调用方式**

```javascript
wx.cloud.callFunction({
  name: 'eventManager',
  data: { action: 'create', eventData: { title, date, type, ... } }
})
```

**action 清单**

| action | 必填参数 | 说明 |
|--------|---------|------|
| create | eventData.title, eventData.date | 创建事件 |
| update | eventId, eventData | 更新事件 |
| delete | eventId | 删除事件 |
| get | eventId | 获取单个事件 |
| list | filter（可选） | 获取事件列表 |
| batchCreate | eventData.events | 批量创建 |

**完整实现**

```javascript
// cloudfunctions/eventManager/index.js
const cloud = require('wx-server-sdk')

cloud.init({ env: 'toust-d9gu1e0km340d2d2f' })
const db = cloud.database()
const _ = db.command

exports.main = async (event, context) => {
  const { OPENID } = cloud.getWXContext()
  const { action, eventData, eventId, filter } = event

  try {
    switch (action) {
      case 'create':
        return await createEvent(OPENID, eventData)
      case 'update':
        return await updateEvent(OPENID, eventId, eventData)
      case 'delete':
        return await deleteEvent(OPENID, eventId)
      case 'get':
        return await getEvent(OPENID, eventId)
      case 'list':
        return await listEvents(OPENID, filter)
      case 'batchCreate':
        return await batchCreateEvents(OPENID, eventData.events)
      default:
        return { code: -1, message: '无效的操作类型' }
    }
  } catch (err) {
    console.error('eventManager error:', err)
    return { code: -1, message: err.message }
  }
}

async function createEvent(openid, data) {
  if (!data.title || !data.title.trim()) {
    return { code: -1, message: '标题不能为空' }
  }
  if (!data.date) {
    return { code: -1, message: '日期不能为空' }
  }

  const now = db.serverDate()
  const result = await db.collection('events').add({
    data: {
      openid,
      title: data.title.trim(),
      date: data.date,
      description: (data.description || '').trim(),
      type: data.type || 'daily',
      photos: data.photos || [],
      music: data.music || null,
      location: data.location || null,
      sort: data.sort || 0,
      createdAt: now,
      updatedAt: now
    }
  })

  return { code: 0, message: '创建成功', data: { _id: result._id } }
}

async function updateEvent(openid, eventId, data) {
  const event = await db.collection('events').doc(eventId).get()
  if (!event.data || event.data.openid !== openid) {
    return { code: -1, message: '无权限操作' }
  }

  const updateData = { updatedAt: db.serverDate() }
  if (data.title !== undefined) updateData.title = data.title.trim()
  if (data.date !== undefined) updateData.date = data.date
  if (data.description !== undefined) updateData.description = data.description.trim()
  if (data.type !== undefined) updateData.type = data.type
  if (data.photos !== undefined) updateData.photos = data.photos
  if (data.music !== undefined) updateData.music = data.music
  if (data.location !== undefined) updateData.location = data.location
  if (data.sort !== undefined) updateData.sort = data.sort

  await db.collection('events').doc(eventId).update({ data: updateData })
  return { code: 0, message: '更新成功' }
}

async function deleteEvent(openid, eventId) {
  const event = await db.collection('events').doc(eventId).get()
  if (!event.data || event.data.openid !== openid) {
    return { code: -1, message: '无权限操作' }
  }

  await db.collection('events').doc(eventId).remove()
  return { code: 0, message: '删除成功' }
}

async function getEvent(openid, eventId) {
  const event = await db.collection('events').doc(eventId).get()
  if (!event.data || event.data.openid !== openid) {
    return { code: -1, message: '事件不存在或无权限' }
  }

  return { code: 0, data: event.data }
}

async function listEvents(openid, filter = {}) {
  let query = { openid }

  if (filter.startDate) {
    query.date = _.gte(filter.startDate)
  }
  if (filter.endDate) {
    query.date = query.date ? _.and([query.date, _.lte(filter.endDate)]) : _.lte(filter.endDate)
  }
  if (filter.type) {
    query.type = filter.type
  }

  const result = await db.collection('events')
    .where(query)
    .orderBy(filter.sortField || 'date', filter.sortOrder || 'asc')
    .skip(filter.skip || 0)
    .limit(filter.limit || 100)
    .get()

  return { code: 0, data: result.data }
}

async function batchCreateEvents(openid, events) {
  const results = await Promise.all(events.map(e => createEvent(openid, e)))
  const successCount = results.filter(r => r.code === 0).length
  return { code: 0, message: `成功创建 ${successCount}/${events.length} 个事件`, data: results }
}
```

### 4.2 uploadManager

> 媒体上传云函数，生成云存储上传凭证

**调用方式**

```javascript
// 1. 获取上传凭证
const res = await wx.cloud.callFunction({
  name: 'uploadManager',
  data: { action: 'getUploadUrl', fileName: 'photo.jpg', eventType: 'daily' }
})

// 2. 使用 wx.cloud.uploadFile 直接上传
const uploadRes = await wx.cloud.uploadFile({
  cloudPath: res.result.data.cloudPath,
  filePath: localFilePath
})
```

**action 清单**

| action | 参数 | 说明 |
|--------|------|------|
| getUploadUrl | fileName, eventType | 生成云存储路径 |
| getTempUrl | fileIds | 批量获取临时链接 |
| deleteFile | fileIds | 批量删除文件 |

**完整实现**

```javascript
// cloudfunctions/uploadManager/index.js
const cloud = require('wx-server-sdk')

cloud.init({ env: 'toust-d9gu1e0km340d2d2f' })

exports.main = async (event, context) => {
  const { OPENID } = cloud.getWXContext()
  const { action, fileName, eventType, fileIds } = event

  try {
    switch (action) {
      case 'getUploadUrl':
        return getUploadUrl(OPENID, fileName, eventType)
      case 'getTempUrl':
        return await getTempUrl(fileIds)
      case 'deleteFile':
        return await deleteFile(OPENID, fileIds)
      default:
        return { code: -1, message: '无效的操作类型' }
    }
  } catch (err) {
    console.error('uploadManager error:', err)
    return { code: -1, message: err.message }
  }
}

function getUploadUrl(openid, fileName, eventType) {
  const timestamp = Date.now()
  const randomStr = Math.random().toString(36).substr(2, 9)
  const ext = fileName.split('.').pop()
  const cloudPath = `images/${openid}/${eventType || 'default'}/${timestamp}_${randomStr}.${ext}`

  return { code: 0, data: { cloudPath } }
}

async function getTempUrl(fileIds) {
  const result = await cloud.getTempFileURL({ fileList: fileIds })
  return {
    code: 0,
    data: result.fileList.map(f => ({
      fileID: f.fileID,
      tempFileURL: f.tempFileURL
    }))
  }
}

async function deleteFile(openid, fileIds) {
  const result = await cloud.deleteFile({ fileList: fileIds })
  return { code: 0, data: result.fileList }
}
```

### 4.3 adManager

> 广告与积分管理云函数

**action 清单**

| action | 参数 | 说明 |
|--------|------|------|
| getPoints | 无 | 查询当前积分 |
| consumePoints | points | 扣除积分 |
| recordAdWatch | 无 | 记录广告观看，+30 积分 |

**完整实现**

```javascript
// cloudfunctions/adManager/index.js
const cloud = require('wx-server-sdk')

cloud.init({ env: 'toust-d9gu1e0km340d2d2f' })
const db = cloud.database()

exports.main = async (event, context) => {
  const { OPENID } = cloud.getWXContext()
  const { action, points } = event

  try {
    switch (action) {
      case 'getPoints':
        return await getPoints(OPENID)
      case 'consumePoints':
        return await consumePoints(OPENID, points)
      case 'recordAdWatch':
        return await recordAdWatch(OPENID)
      default:
        return { code: -1, message: '无效的操作类型' }
    }
  } catch (err) {
    console.error('adManager error:', err)
    return { code: -1, message: err.message }
  }
}

async function getPoints(openid) {
  const user = await db.collection('users').where({ openid }).get()
  if (!user.data.length) return { code: -1, message: '用户不存在' }

  return {
    code: 0,
    data: {
      points: user.data[0].points || 0,
      adWatchCount: user.data[0].adWatchCount || 0
    }
  }
}

async function consumePoints(openid, points) {
  const user = await db.collection('users').where({ openid }).get()
  if (!user.data.length) return { code: -1, message: '用户不存在' }

  const current = user.data[0].points || 0
  if (current < points) {
    return { code: -1, message: '积分不足', data: { points: current } }
  }

  await db.collection('users').doc(user.data[0]._id).update({
    data: { points: current - points }
  })

  return { code: 0, data: { points: current - points } }
}

async function recordAdWatch(openid) {
  const user = await db.collection('users').where({ openid }).get()
  if (!user.data.length) return { code: -1, message: '用户不存在' }

  const current = user.data[0].points || 0
  const newPoints = current + 30

  await db.collection('users').doc(user.data[0]._id).update({
    data: {
      points: newPoints,
      adWatchCount: (user.data[0].adWatchCount || 0) + 1,
      lastAdWatchTime: db.serverDate()
    }
  })

  return { code: 0, message: '获得30积分', data: { points: newPoints } }
}
```

### 4.4 shareManager

> 分享管理云函数

```javascript
// cloudfunctions/shareManager/index.js
const cloud = require('wx-server-sdk')

cloud.init({ env: 'toust-d9gu1e0km340d2d2f' })
const db = cloud.database()

exports.main = async (event, context) => {
  const { OPENID } = cloud.getWXContext()
  const { action, eventId } = event

  try {
    switch (action) {
      case 'generateShareCard':
        return await generateShareCard(OPENID, eventId)
      case 'recordShare':
        return await recordShare(OPENID, eventId)
      default:
        return { code: -1, message: '无效的操作类型' }
    }
  } catch (err) {
    console.error('shareManager error:', err)
    return { code: -1, message: err.message }
  }
}

async function generateShareCard(openid, eventId) {
  const event = await db.collection('events').doc(eventId).get()
  if (!event.data || event.data.openid !== openid) {
    return { code: -1, message: '事件不存在或无权限' }
  }

  const user = await db.collection('users').where({ openid }).get()

  return {
    code: 0,
    data: { event: event.data, userInfo: user.data[0] }
  }
}

async function recordShare(openid, eventId) {
  await db.collection('share_records').add({
    data: { openid, eventId, sharedAt: db.serverDate() }
  })
  return { code: 0, message: '记录成功' }
}
```

### 4.5 pdfExporter

> PDF 导出云函数

```javascript
// cloudfunctions/pdfExporter/index.js
const cloud = require('wx-server-sdk')

cloud.init({ env: 'toust-d9gu1e0km340d2d2f' })
const db = cloud.database()

exports.main = async (event, context) => {
  const { OPENID } = cloud.getWXContext()
  const { includePhotos } = event

  try {
    const events = await db.collection('events')
      .where({ openid: OPENID })
      .orderBy('date', 'asc')
      .get()

    const user = await db.collection('users').where({ openid: OPENID }).get()

    return {
      code: 0,
      data: {
        userInfo: user.data[0],
        events: events.data,
        includePhotos: includePhotos !== false
      }
    }
  } catch (err) {
    console.error('pdfExporter error:', err)
    return { code: -1, message: err.message }
  }
}
```

---

## 五、前端页面开发

### 5.1 event-edit 页面

> 事件创建/编辑表单页

**路由配置**（app.json 页面注册）

```json
"pages/event-edit/event-edit"
```

**页面接口**

```typescript
interface PageData {
  eventId: string              // 编辑模式下的事件 ID
  isEdit: boolean              // 是否编辑模式
  title: string
  date: string
  description: string
  type: 'daily' | 'birthday' | 'anniversary' | 'special'
  photos: PhotoItem[]
  musicTitle: string
  musicArtist: string
  musicUrl: string
  locationName: string
  submitting: boolean
  typeOptions: Array<{ value: string; label: string }>
}

interface PhotoItem {
  fileId?: string
  tempFilePath?: string
  description: string
  uploading: boolean
  progress: number
}
```

**核心方法**

```typescript
// 保存事件
async saveEvent() {
  if (!this.data.title.trim()) {
    wx.showToast({ title: '请输入标题', icon: 'none' })
    return
  }
  if (!this.data.date) {
    wx.showToast({ title: '请选择日期', icon: 'none' })
    return
  }

  this.setData({ submitting: true })

  const eventData = {
    title: this.data.title,
    date: this.data.date,
    description: this.data.description,
    type: this.data.type,
    photos: this.data.photos.filter(p => p.fileId).map((p, i) => ({
      fileId: p.fileId,
      description: p.description,
      order: i
    })),
    music: this.data.musicTitle ? {
      title: this.data.musicTitle,
      artist: this.data.musicArtist,
      url: this.data.musicUrl
    } : null
  }

  const action = this.data.isEdit ? 'update' : 'create'
  const params: any = { action, eventData }
  if (this.data.isEdit) params.eventId = this.data.eventId

  const res = await wx.cloud.callFunction({ name: 'eventManager', data: params })

  this.setData({ submitting: false })

  if (res.result?.code === 0) {
    wx.showToast({ title: '保存成功', icon: 'success' })
    setTimeout(() => wx.navigateBack(), 1500)
  } else {
    wx.showToast({ title: res.result?.message || '保存失败', icon: 'none' })
  }
}

// 选择并上传照片
async choosePhoto() {
  const remaining = 9 - this.data.photos.length
  if (remaining <= 0) {
    wx.showToast({ title: '最多9张照片', icon: 'none' })
    return
  }

  const res = await wx.chooseMedia({
    count: remaining,
    mediaType: ['image'],
    sizeType: ['compressed']
  })

  const newPhotos = res.tempFiles.map(f => ({
    tempFilePath: f.tempFilePath,
    description: '',
    uploading: true,
    progress: 0
  }))

  this.setData({ photos: [...this.data.photos, ...newPhotos] })

  for (let i = this.data.photos.length - newPhotos.length; i < this.data.photos.length; i++) {
    await this.uploadPhoto(i)
  }
}

// 上传单张照片
async uploadPhoto(index: number) {
  const photo = this.data.photos[index]
  const fileName = photo.tempFilePath.split('/').pop()

  const urlRes = await wx.cloud.callFunction({
    name: 'uploadManager',
    data: { action: 'getUploadUrl', fileName, eventType: this.data.type }
  })

  if (urlRes.result?.code !== 0) {
    this.updatePhoto(index, { uploading: false })
    return
  }

  const uploadRes = await wx.cloud.uploadFile({
    cloudPath: urlRes.result.data.cloudPath,
    filePath: photo.tempFilePath
  })

  this.updatePhoto(index, {
    fileId: uploadRes.fileID,
    uploading: false,
    progress: 100
  })
}

// 删除照片
deletePhoto(index: number) {
  const photos = [...this.data.photos]
  photos.splice(index, 1)
  this.setData({ photos })
}
```

### 5.2 home 页面事件集成

> 在现有 home.ts 中集成事件数据加载

```typescript
// home.ts - 加载事件列表
async fetchEvents() {
  const res = await wx.cloud.callFunction({
    name: 'eventManager',
    data: { action: 'list', filter: { sortField: 'date', sortOrder: 'asc' } }
  })

  if (res.result?.code === 0) {
    const events = res.result.data
    this.setData({
      events,
      totalPages: Math.max(1, Math.ceil(events.length / 1))
    })
  }
}

// 跳转到事件编辑页
editPage() {
  const currentEvent = this.data.events[this.data.currentPage - 1]
  const url = currentEvent
    ? `/pages/event-edit/event-edit?id=${currentEvent._id}`
    : '/pages/event-edit/event-edit'
  wx.navigateTo({ url })
}

// 删除事件
async deleteEvent(eventId: string) {
  const modalRes = await wx.showModal({
    title: '确认删除',
    content: '删除后无法恢复，确定删除吗？'
  })

  if (!modalRes.confirm) return

  const res = await wx.cloud.callFunction({
    name: 'eventManager',
    data: { action: 'delete', eventId }
  })

  if (res.result?.code === 0) {
    wx.showToast({ title: '已删除', icon: 'success' })
    this.fetchEvents()
  }
}
```

### 5.3 mini-player 组件

> 底部迷你音乐播放器

**组件接口**

```typescript
interface MiniPlayerProps {
  musicList: MusicTrack[]
  currentTrack: number
}

interface MusicTrack {
  title: string
  artist: string
  src: string
}

interface MiniPlayerData {
  isPlaying: boolean
  currentTime: number
  duration: number
  progress: number
}
```

**核心实现**

```typescript
// components/mini-player/mini-player.ts
const backgroundAudioManager = wx.getBackgroundAudioManager()

Component({
  properties: {
    musicList: { type: Array, value: [] },
    currentTrack: { type: Number, value: 0 }
  },

  data: {
    isPlaying: false,
    currentTime: 0,
    duration: 0,
    progress: 0
  },

  lifetimes: {
    attached() {
      this.setupAudioListeners()
    }
  },

  methods: {
    setupAudioListeners() {
      backgroundAudioManager.onPlay(() => this.setData({ isPlaying: true }))
      backgroundAudioManager.onPause(() => this.setData({ isPlaying: false }))
      backgroundAudioManager.onStop(() => this.setData({ isPlaying: false, currentTime: 0, progress: 0 }))
      backgroundAudioManager.onTimeUpdate(() => {
        const { currentTime, duration } = backgroundAudioManager
        this.setData({
          currentTime,
          duration,
          progress: duration > 0 ? (currentTime / duration) * 100 : 0
        })
      })
      backgroundAudioManager.onEnded(() => this.nextTrack())
    },

    togglePlay() {
      if (this.data.isPlaying) {
        backgroundAudioManager.pause()
      } else {
        const track = this.properties.musicList[this.properties.currentTrack]
        if (track) {
          backgroundAudioManager.title = track.title
          backgroundAudioManager.singer = track.artist
          backgroundAudioManager.src = track.src
        }
      }
    },

    nextTrack() {
      const list = this.properties.musicList
      const next = (this.properties.currentTrack + 1) % list.length
      this.triggerEvent('trackchange', { index: next })
    },

    prevTrack() {
      const list = this.properties.musicList
      const prev = (this.properties.currentTrack - 1 + list.length) % list.length
      this.triggerEvent('trackchange', { index: prev })
    },

    onProgressChange(e: any) {
      const { value } = e.detail
      const seekTime = (value / 100) * this.data.duration
      backgroundAudioManager.seek(seekTime)
    },

    formatTime(seconds: number): string {
      const min = Math.floor(seconds / 60)
      const sec = Math.floor(seconds % 60)
      return `${min}:${sec.toString().padStart(2, '0')}`
    }
  }
})
```

### 5.4 分享海报前端

```typescript
// home.ts - 分享海报
async sharePage() {
  const currentEvent = this.data.events[this.data.currentPage - 1]
  if (!currentEvent) {
    wx.showToast({ title: '当前无事件', icon: 'none' })
    return
  }

  wx.showLoading({ title: '生成海报中...' })

  const res = await wx.cloud.callFunction({
    name: 'shareManager',
    data: { action: 'generateShareCard', eventId: currentEvent._id }
  })

  wx.hideLoading()

  if (res.result?.code === 0) {
    await this.drawPoster(res.result.data)
  } else {
    wx.showToast({ title: res.result?.message || '生成失败', icon: 'none' })
  }
},

async drawPoster(data: any) {
  const ctx = wx.createCanvasContext('posterCanvas')

  ctx.setFillStyle('#0f1023')
  ctx.fillRect(0, 0, 750, 1334)

  ctx.setStrokeStyle('#C9A96E')
  ctx.setLineWidth(2)
  for (let i = 0; i < 5; i++) {
    ctx.moveTo(50, 200 + i * 40)
    ctx.lineTo(700, 200 + i * 40)
    ctx.stroke()
  }

  ctx.setFillStyle('#FFF8F0')
  ctx.setFontSize(48)
  ctx.fillText(data.event.title, 100, 150)

  ctx.setFillStyle('#C9A96E')
  ctx.setFontSize(32)
  ctx.fillText(data.event.date, 100, 500)

  ctx.setFillStyle('#FFF8F0')
  ctx.setFontSize(28)
  ctx.fillText(data.event.description || '', 100, 560)

  ctx.setFillStyle('#C9A96E')
  ctx.setFontSize(24)
  ctx.fillText('和弦之约', 300, 1250)

  ctx.draw()

  setTimeout(() => {
    wx.canvasToTempFilePath({
      canvasId: 'posterCanvas',
      quality: 1,
      success: (res) => {
        wx.showActionSheet({
          itemList: ['保存相册', '分享给好友'],
          success: async (action) => {
            if (action.tapIndex === 0) {
              wx.saveImageToPhotosAlbum({
                filePath: res.tempFilePath,
                success: () => wx.showToast({ title: '已保存', icon: 'success' }),
                fail: (err) => {
                  if (err.errMsg.includes('auth deny')) {
                    wx.showModal({
                      title: '提示',
                      content: '需要相册权限',
                      confirmText: '去设置',
                      success: (r) => { if (r.confirm) wx.openSetting() }
                    })
                  }
                }
              })
            }
          }
        })
      }
    })
  }, 500)
}
```

### 5.5 广告接入前端

```typescript
// home.ts - 下载与广告
async downloadPage() {
  const pointsRes = await wx.cloud.callFunction({
    name: 'adManager',
    data: { action: 'getPoints' }
  })

  const currentPoints = pointsRes.result?.data?.points || 0

  wx.showActionSheet({
    itemList: [`花费30积分（当前${currentPoints}）`, '观看广告获得30积分'],
    success: async (res) => {
      if (res.tapIndex === 0) {
        if (currentPoints < 30) {
          wx.showToast({ title: '积分不足，请先观看广告', icon: 'none' })
          return
        }
        await this.consumePointsAndDownload()
      } else {
        this.showRewardedVideoAd()
      }
    }
  })
},

async consumePointsAndDownload() {
  const res = await wx.cloud.callFunction({
    name: 'adManager',
    data: { action: 'consumePoints', points: 30 }
  })

  if (res.result?.code === 0) {
    wx.showToast({ title: '下载开始', icon: 'success' })
  } else {
    wx.showToast({ title: res.result?.message || '操作失败', icon: 'none' })
  }
},

showRewardedVideoAd() {
  const videoAd = wx.createRewardedVideoAd({ adUnitId: 'adunit-xxxxx' })

  videoAd.onClose((res) => {
    if (res?.isEnded) {
      wx.cloud.callFunction({
        name: 'adManager',
        data: { action: 'recordAdWatch' }
      }).then(r => {
        if (r.result?.code === 0) {
          wx.showToast({ title: `获得${r.result.data.points}积分`, icon: 'success' })
        }
      })
    } else {
      wx.showToast({ title: '未观看完整', icon: 'none' })
    }
  })

  videoAd.show().catch(() => {
    videoAd.load().then(() => videoAd.show())
  })
}
```

### 5.6 PDF 导出前端

```typescript
// home.ts - PDF 导出
async exportPDF() {
  wx.showLoading({ title: '生成中...' })

  const res = await wx.cloud.callFunction({
    name: 'pdfExporter',
    data: { includePhotos: true }
  })

  wx.hideLoading()

  if (res.result?.code === 0) {
    const ctx = wx.createCanvasContext('pdfCanvas')
    const { userInfo, events } = res.result.data

    ctx.setFillStyle('#0f1023')
    ctx.fillRect(0, 0, 595, 842)

    ctx.setFillStyle('#FFF8F0')
    ctx.setFontSize(32)
    ctx.fillText('和弦之约', 200, 100)

    let y = 200
    events.forEach((event: any) => {
      ctx.setFillStyle('#C9A96E')
      ctx.setFontSize(18)
      ctx.fillText(event.title, 50, y)
      ctx.setFillStyle('#FFF8F0')
      ctx.setFontSize(14)
      ctx.fillText(event.date, 50, y + 25)
      y += 80
    })

    ctx.draw()

    setTimeout(() => {
      wx.canvasToTempFilePath({
        canvasId: 'pdfCanvas',
        success: (res) => {
          wx.saveImageToPhotosAlbum({
            filePath: res.tempFilePath,
            success: () => wx.showToast({ title: '已保存', icon: 'success' })
          })
        }
      })
    }, 500)
  } else {
    wx.showToast({ title: '导出失败', icon: 'none' })
  }
}
```

---

## 六、3D 翻页交互

### CSS 实现

```css
/* pages/home/home.wxss */
.page-flip-container {
  perspective: 1500px;
  transform-style: preserve-3d;
}

.swiper-item {
  transform-origin: center center;
  transition: transform 0.5s ease;
}

.swiper-item.flip-left {
  animation: flipLeft 0.5s ease forwards;
}

.swiper-item.flip-right {
  animation: flipRight 0.5s ease forwards;
}

@keyframes flipLeft {
  0%   { transform: rotateY(0) scale(1); opacity: 1; }
  50%  { transform: rotateY(-45deg) scale(0.95); opacity: 0.8; }
  100% { transform: rotateY(-90deg) scale(0.9); opacity: 0; }
}

@keyframes flipRight {
  0%   { transform: rotateY(0) scale(1); opacity: 1; }
  50%  { transform: rotateY(45deg) scale(0.95); opacity: 0.8; }
  100% { transform: rotateY(90deg) scale(0.9); opacity: 0; }
}
```

### 交互逻辑

```typescript
// home.ts - 翻页方法（已有基础实现，需增强）
prevPage() {
  if (!this.data.canFlip || this.data.currentPage === 0) return
  this.triggerFlip('flip-right', () => {
    this.setData({ currentPage: this.data.currentPage - 1 })
  })
},

nextPage() {
  if (!this.data.canFlip || this.data.currentPage >= this.data.totalPages - 1) return
  this.triggerFlip('flip-left', () => {
    this.setData({ currentPage: this.data.currentPage + 1 })
  })
},

triggerFlip(animation: string, callback: Function) {
  this.setData({ flipAnimation: animation, canFlip: false })
  setTimeout(() => {
    callback()
    this.setData({ flipAnimation: '' })
    setTimeout(() => this.setData({ canFlip: true }), 100)
  }, 500)
}
```

---

## 七、测试验收清单

### 功能测试

| 测试项 | 验收标准 | 状态 |
|--------|---------|------|
| 事件创建 | 填写表单 → 保存 → 数据写入 events 集合 |  |
| 事件编辑 | 修改字段 → 保存 → 数据更新 |  |
| 事件删除 | 点击删除 → 二次确认 → 数据删除 |  |
| 照片上传 | 选择照片 → 压缩 → 上传 → 显示预览 |  |
| 乐谱渲染 | 五线谱 + 音符卡片正确显示 |  |
| 翻页交互 | 3D 动画流畅，防抖生效 |  |
| 音乐播放 | 播放/暂停/切换/进度条 |  |
| 分享海报 | Canvas 绘制 → 保存相册 |  |
| 广告积分 | 观看广告 +30 积分，下载 -30 积分 |  |
| PDF 导出 | 生成 → 保存 |  |

### 性能测试

| 指标 | 目标 |
|------|------|
| 首屏加载 | ≤ 2s |
| 页面切换 | ≤ 300ms |
| 海报生成 | ≤ 2s |

### 兼容性

- iPhone 13/14/15/16, Android 主流机型
- iOS 15+, Android 10+
- 微信 8.0+

---

## 八、风险控制

| 风险 | 影响 | 应对策略 |
|------|------|---------|
| 3D 翻页性能 | 低端机卡顿 | 自动降级为 2D 滑动切换 |
| Canvas 兼容性 | 部分机型不支持 | 降级为 WXML 渲染 |
| 云函数超时 | 操作失败 | 重试机制 + 查询优化 |
| 广告加载失败 | 无法获取积分 | 提供积分不足提示 + 重试 |

---

**文档版本：v6.0**  
**更新日期：2026-05-03**
